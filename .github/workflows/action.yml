name: github-action-libft

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt install siege -y
      - run: make debug
      - run: siege -c 2 -r 100 -b -t 10s
